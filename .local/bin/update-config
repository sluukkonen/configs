#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd -P "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" >/dev/null 2>&1 && pwd)"

cd "$SCRIPT_DIR/../.."

git pull

./init.sh

zsh -c 'source "$HOME/.zgen/zgen.zsh"; zgen selfupdate'
zsh -c 'source "$HOME/.zgen/zgen.zsh"; zgen update'

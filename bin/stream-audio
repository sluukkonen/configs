#!/bin/bash
#
# Requires esound (brew install esound) and SoundFlower
# (http://code.google.com/p/soundflower/) set up as the default audio input and
# output device.
#
# Adapted from https://labitat.dk/wiki/Apple/Mac_OS_X

esd -tcp -bind ::1 & sleep 1 && (esdrec -s ::1 | esdcat -s thor.local)

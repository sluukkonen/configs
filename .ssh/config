# UseKeychain is Apple-specific. Linux OpenSSH still parses inactive Match
# blocks, so ignore it explicitly and keep this before any Host/Match blocks.
IgnoreUnknown UseKeychain

Host *
  SetEnv TERM=xterm-256color
  AddKeysToAgent yes
  UseKeychain yes

Include config_*
Include ~/.colima/ssh_config
